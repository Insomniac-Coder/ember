error[E3060]: `text` does not live long enough
 --> tests/ui/borrow/B7/borrow_outlives_value_parameter.em:2:12
  |
1 | fn borrow_text(owned text: String) -> str:
  |                ------------------ `text` is `owned`, so it is the callee's own and its storage ends with the frame
2 |     return text
  |            ^^^^ the borrow is still live when the function returns
  |            ---- `text` is borrowed here
  |
  = help: borrow `text` instead of taking it `owned`, or return an owned value
  = note: a returned reference must derive from a parameter (LT-1)

error: could not compile due to 1 error(s)
