# Session record — 2026-09-09, applying v0.8.3

Written to be read cold. `docs/HANDOFF.md` is the standing document; this is
what one session did, what it found, and what it left. Read
`docs/MIGRATION-0.8.3.md` for the analysis that decided the order.

---

## 1. The one-paragraph version

The owner supplied **Ember v0.8.3**, seven revisions ahead of the v0.5 the
repository was on. It is installed as the normative specification, seventeen
defects in it are recorded with the reading taken, and Ember Core
(`[CONF-2]` — Parts II–VII, XIII and `[RNG-*]`) is substantially further along:
range types, `Span`/`MutSpan`, a real `std` package, field visibility, function
values and capture-free closures all landed, and **twelve compiler defects**
were found and eleven fixed. Everything is green; nothing is half-applied.

---

## 2. State of the tree

**Branch `main`, 22 commits ahead of `origin/main`: 12 from this session on
top of the 10 that were already unpushed. Nothing is pushed.** Working tree
clean.

    176 tests green (cargo test --workspace)
    0 warnings (cargo build)
    5 gates green:
      python tools/rule_index.py          833 rules, 804 without a test dir (baselined)
      python tools/spec_check.py          19 of 44 fenced blocks parse, 25 baselined
      python tools/check_branding.py      13 known fixture names
      python tools/split_spec.py --check  docs/spec/ is the split of the source
      python tools/error_pages.py         3 pages, 59 emitted codes without one (baselined)

**Build and run**

    cargo build                                     # debug
    cargo build --release                           # spec_check and error_pages need this
    ./target/debug/ember run <file.em>
    ./target/debug/ember check <file.em>
    ./target/debug/ember check --syntax-only <file.em>
    ./target/debug/ember build <file.em> --emit c|mir|hir|ast

`EMBER_STD` overrides where `std`'s sources are found; otherwise
`ember_branding::std_root()` walks up from the executable and the working
directory looking for `std/src`.

---

## 3. The specification

| Path | What |
|---|---|
| `docs/spec-source/as-received/Ember_v0.8.3_spec.md` | byte-identical to what the owner sent. **Never edit.** |
| `docs/spec-source/ember-spec.md` | normative: as-received plus the applied errata |
| `docs/spec/` | generated by `tools/split_spec.py`. **Never hand-edit.** |
| `docs/spec-errata.md` | ERR-001..024 (v0.5, superseded), **ERR-025..041 (v0.8.3)** |
| `docs/DECISIONS.md` | ADR-001..012 (earlier), **ADR-013..018 (this session)** |
| `docs/DEFECTS.md` | D-001..013 (earlier), **D-014..025 (this session)** |
| `docs/MIGRATION-0.8.3.md` | the gap analysis and the ordered route |
| `docs/RFC-v0.6.md` | **history, not a plan** — 0.6.2 removed the layer it argues for |

**Facts worth not re-deriving:**

* **No v0.5 rule id is absent from v0.8.3.** Checked mechanically. Nothing that
  was implemented was invalidated.
* Parts renumbered: old XVIII (Compiler Architecture) → **XIX**, XIX
  (Toolchain) → **XX**, XX (Implementation Plan) → **XXI**, XXI (RageV) →
  **XXII**, XXII (Open Questions) → **XXIII**. The new **XVIII is Hot Reload**.
* **Contracts are gone** (0.6.2, `OQ-27`): no `@requires`, `@ensures`,
  `@invariant`, `@decreases`, `@verified`, `@assume`, no `[CTR-*]`/`[PRV-*]`,
  no solver, no proof manifest.
* Five sections are printed inside a Part they are not numbered for — `[WK-4]`
  is in Part XV, `[FFI-43]` in Part XX, `[CXX-*]` in Part XVI, `[BUD-*]` in
  Part XX. **Find a rule by its id, not by its Part.** `docs/spec/README.md`
  carries the table.

### The two questions left for the owner

Neither was decided silently. The grammar is **not** changed until they rule.

* **ERR-036** — Part III §2's `fn_header` has no `extern` prefix, so
  `pub extern "C" fn on_update(…)` parses under no production, though XVI.10
  writes it and `[FFI-26]`, `[FFI-28]`, `[FFI-31b]` and `[HR-21]` depend on it.
  Provisional reading: the prefix belongs on `fn_header` (**ADR-014**).
* **ERR-037** — no `extern class` production, though `[FFI-39]` spends forty
  lines specifying one. **No provisional reading taken** — there is no
  smallest-change answer, and writing a production would decide the shape of
  the C++ inheritance surface silently.

Both blocks sit in `[TST-7]`'s baseline. Phase 5/7; nothing before them
depends on the answer.

---

## 4. What was built, with the rules

| Landed | Rules |
|---|---|
| `yield` a v1 keyword (49, not 48); `gen` contextual | `[LEX-15b]`, ERR-025 |
| `range_clause`, `gen fn`, `yield` parse | `[GRM-8d]`, `[GRM-21]`, `[GRM-22]` |
| 62 diagnostic codes registered; registry exhaustive | `[DIA-6a]` |
| **Range and domain types**, all of §IV.2a bar `[RNG-4]` | `[RNG-1..10]`, `[TYP-5]` |
| **`Span[T]` / `MutSpan[T]`**, Part IV's View category in full | `[SPN-1..3]`, `[TYP-15]` |
| `std` a resolvable package: `lib`, `core`, `math` | `[MOD-1]`, `[STD-1]` |
| `Self` in every signature; whole-program interfaces; imported bounds | Part IV §8, `[IFC-1]`, `[TYP-17]` |
| Field visibility, `pub(read)`, the memberwise constructor's | `[MOD-2]`, `[MOD-7]`, `[STR-1]` |
| `fn(A) -> R` types, function values, capture-free closures | `[FN-6]`, `[CLO-1]`, `[CLO-3]` |
| `[LEX-6a]`'s bracketed closure body, `E0106` | `[GRM-17]` |
| Eleven language versions accepted | `[MOD-6]` |
| The conformance suite is **run**; three ratcheting baselines | `[TST-4]`, `[TST-4a]`, `[TST-4c]`, `[DIA-6]` |

**33 conformance directories** under `tests/conformance/`, each with
`[TST-4a]`'s accept **and** reject cases, executed by
`compiler/ember_driver/tests/milestones.rs::the_conformance_suite_runs`.
Annotation kinds: `run-pass`, `compile-fail`, `parse-pass`, `parse-fail`
(the last two run `ember check --syntax-only`, for a rule whose grammar landed
ahead of its semantics).

---

## 5. Defects found in the compiler (D-014..D-025)

Every one of these let a program do something the specification forbids.

| # | What | Status |
|---|---|---|
| D-014 | a `##` doc comment above an `import` was `E0100`; `[LEX-11]` says it is discarded in silence | fixed |
| D-015 | interfaces collected **per module**, so a module checked first could not implement one declared later. `[MOD-4]` allows import cycles, so no load order would have worked | fixed |
| D-016 | a bound, an `implements` clause and a supertrait each recorded the name **as written**, so an imported `Ord` did not match its implementation | fixed |
| D-017 | `Self` resolved in no signature, so Part IV §8's nine interfaces could not be written at all | fixed |
| D-018 | `[RNG-4]`'s range tracking derives a range for **constants only** | **open — precision, not soundness** |
| D-019 | **field visibility was not enforced at all** — every field of every struct read from every module | fixed |
| D-020 | `pub(read)` was parsed and dropped; `E1050` was emitted by nobody | fixed |
| D-021 | `[STR-1]`'s memberwise constructor ignored its fields' visibility, so a `pub(read)` field could be written by constructing | fixed |
| D-022 | **`[SPN-1]`'s coercion took no borrow**, so `v: Span[i32] = a` then `a.push(…)` compiled and read freed memory — a use-after-free from Safe Ember | fixed |
| D-023 | `lower_span_get` assumed `Some` was `Option`'s variant 0; `option_of` builds `None` first | fixed |
| D-024 | a call argument parsed as a full expression, so a lambda's `:` body inside brackets swallowed a block | fixed |
| D-025 | `RangeError` is not a name a program can write, so `[RNG-3]`'s own signature cannot be written | **open — small** |

**D-022 is the one to remember.** It was found by *asking whether the borrow
checker saw the view*, not by a test going red — nothing tested it, because the
feature was an hour old. The fix: `[SPN-1]`'s coercion emits
`SpanFrom(&container)`, because `Rvalue::Ref` is what `collect_loans` looks
for, and the call terminator's elision carries the loan's region to the view.

---

## 6. Open problems, in the order they should be picked up

### 6.0 The deviation register — everything that differs from v0.8.3

Audited in full on the owner's instruction, 2026-09-09, after the rule that
**the specification is never altered to fit the compiler**. Three kinds appear
below and the difference matters:

* a **reading** resolves something the document leaves open or contradicts
  itself about — no code change is owed;
* a **deviation** departs from text that is clear — code change owed;
* a **gap** is a rule with nothing built against it — not a deviation, but the
  larger number by far.

**Scale first, so the rest is in proportion.** The document states **833 rules**.
**30** have a conformance directory. Everything in Parts X–XIV and XVI–XXIII is
unimplemented, so the register below covers the ~4% of the document the
compiler has actually reached.

#### Live deviations, in severity order

| # | Rule | What the document says | What the compiler does |
|---|---|---|---|
| 1 | `[CLO-3]` (ADR-018) | a parameter `f: fn(A) -> R` "is a generic over `Callable` (static dispatch, monomorphised)" | a C function pointer. **A lambda that captures anything is rejected.** §6.1 has the design; the owner has ordered it closed |
| 2 | `[GRM-23]` (ERR-026) | `a in b in c` is **`E0104`** | emits **`E0102`**. One line. `E0102` is what Part III §5's table gives every other operator on that row, which is why the erratum chose it — but the rule is explicit and the document wins |
| 3 | `[MAN-3]` (ERR-039) | `[lints]` naming an unknown lint is **`E9010`** | registers **`E9012`**. `E9010` is also `[TYP-9c]`'s, and `[DIA-6a]` wants one code per rule. Registered only, never emitted, so nothing observable turns on it yet |
| 4 | `[RNG-5a1]` (ADR-016) | the compiler **generates impls** `T: Add[T, Output = R]` etc. in `T`'s declaring module | a type-checker special case. Every program is accepted or rejected as the rule requires and every result has the type it requires. Unobservable **only because no operator interface exists at all** — there is no `Add` to bound on. It becomes observable the day one does |
| 5 | `[LT-2]` (ADR-012) | `E3064` | never emitted. No program reaching it has been found; the compiler narrows instead of refusing |

#### Readings of places the document contradicts itself or is silent

| # | Rule | The contradiction | The reading |
|---|---|---|---|
| 6 | `[FN-1]` vs Part VII §7 (ADR-017, ERR-041) | `[FN-1]`: a `mut` argument "MUST be a mutable place". Part VII §7 passes `buf.as_mut_span()`, a call result | a `mut` view parameter takes the view **by value**; the place requirement lands on what the view was taken of. Only this compiles the document's own example |
| 7 | `[CORO-*]` (ADR-015) | no code is named for `yield` in operand position | `E0100` |
| 8 | `[FFI-17d]` (ERR-034) | cites `[FFI-17b]` for `@ffi(no_virtual_dtor)`, which that rule does not define, and no rule does | nothing implemented — Phase 7. **Open for the owner** |
| 9 | Part III §2 (ERR-036) | XVI.10 writes `pub extern "C" fn on_update(...)`; no production admits it | grammar deliberately unchanged. **Open for the owner** |
| 10 | Part III §2 (ERR-037) | `[FFI-39]` depends on `extern class`; no production admits it | nothing implemented. **Open for the owner** |
| 11 | `[RNG-8]` (ERR-029) | the rule's text **opens mid-sentence on an ellipsis**, and its missing first half survives nowhere in the file | cannot be restored, only guessed. **Open for the owner** — this one needs the owner's own words |

#### What is *not* a deviation, checked and cleared

Recorded because each looked like one:

* **`MutSpan[T]` accepting two calls** — `[SPN-3]` says move-only, and it is:
  a plain move then use is `E3040`. Two `take(m)` calls are the *implicit
  reborrow at a `mut` site* the same rule permits.
* **`yield` in the reserved-for-future list** — `[LEX-15b]` settles it inside
  the document ("this rule supersedes that count", 49 keywords), and the lexer
  already has 49. ERR-025 is withdrawn; the edit was redundant, not corrective.
* **`Roughness + Metallic`** — `E2214`, as `[RNG-5a1]` requires. The mechanism
  differs (row 4), the behaviour does not.

#### The document itself was edited, and has been restored

Fourteen passages of `docs/spec-source/ember-spec.md` had been edited in place
to carry the reading their erratum records. **All fourteen are reverted**; the
file is byte-identical to as-received again (md5
`2bdffee6510b8668cf828185266efedb`) and `docs/spec/` is regenerated from it.
The commit that reverted them lists every one.

Two of those edits were hiding a gate failure, and the failures are now
baselined instead, with the reason in `spec-errata.md`: XVI.4 and XVI.7a fence
overlay-language source as `ember` when Part III defines no `overlay_decl`
(ERR-032), and XX §6's prose names five codes 0.6.2 removed with the contract
layer (ERR-027).

One edit was worse than the others and is worth naming on its own: **`[RNG-8]`'s
opening was not restored from anywhere — I wrote it.** The words "A range type
erases to its representation at every coercion site (`[TYP-5]`) and" appear
nowhere in the owner's file. That is row 11.

### 6.1 The deviation that matters — ADR-018

`[CLO-3]`: "A parameter declared `f: fn(A) -> R` is a generic over `Callable`
(static dispatch, monomorphised)." This compiler makes `TyKind::Fn` a
**concrete function pointer**. Consequences:

* a **capturing closure is refused by name** — it has no environment to live in;
* a call through one is an **indirect** call, where `[COST-3]` classes a
  specialised generic call as *not observable* (a direct call).

**The upgrade is the same work as capturing closures**, and three of its four
steps are existing machinery:

1. a closure literal builds a **synthetic struct** of its captures — `ref T`
   for a read-only capture, `ref mut T` for a mutated one (`[CLO-2]`) — with a
   synthesised `call` method. `[CLO-1]`'s "view type if it captures by
   reference" then falls out of `is_view`, and `[CLO-4]` out of `[TYP-15]`;
2. **new work**: a parameter written `f: fn(A) -> R` becomes a synthetic
   generic parameter carrying that signature as its bound;
3. `f(args)` resolves to the parameter's `call` — `synth_bound_method` already
   does this for a bound's method;
4. instantiation re-checks the body with the parameter bound to the closure's
   struct — `check_instantiations` already does this for every other generic.

**Design notes from the aborted first attempt**, so the next one does not
re-derive them:

* The **generic body is never emitted** — only its instantiations are, and
  `check_instantiations` re-checks the AST with `type_params` bound to concrete
  types. So the generic-level check of `f(args)` only has to *type-check*; it
  need not produce a body that lowers.
* `GenericParam` needs a third field beside `name` and `bounds`: the callable
  signature. `Callable[Args, R]` is a **generic** interface and `InterfaceDef`
  carries no generics, so the bound cannot be a name in `bounds`. `[CLO-6]`
  makes `owned f:` bind `CallableOnce` and `f:`/`mut f:` bind `Callable`, so
  the field wants a `once: bool` too — "the mode already selects the bound".
* Reuse `TyKind::Struct` for the closure's environment rather than adding a
  kind: layout, `is_view` (a `ref` field makes it one, which is exactly
  `[CLO-1]`'s "view type if it captures anything by reference"), `is_copy`,
  drop glue and codegen all follow for free, and `[CLO-4]`'s escape rule
  becomes `[TYP-15]` with nothing added.
* Capture inference wants a **pre-scan of the lambda's AST** for identifiers
  that resolve in the enclosing scope, before the body is checked. Then the
  captured names are bound as ordinary locals initialised from the environment
  fields at the top of the body, so every later read is an ordinary read and
  nothing in the checker needs to know about closures.
* A **named function** should get its own zero-sized type (`FnDef(DefId)`) so
  that instantiating with it produces a **direct** call — `[COST-3]` classes a
  specialised generic call as *not observable*. `TyKind::Fn` then keeps its
  narrower job: `[FN-6]`'s explicit coercion target and `extern "C" fn`.

### 6.2 The rest of Ember Core (`[CONF-2]`), roughly in dependency order

* **`Cell` / `RefCell`** (`[CELL-1..11]`) and **`Arena`** (`[ARN-1..7]`) both
  need the same missing primitive: **interior mutability**. `[CELL-1]`'s `set`
  takes `self` (a *shared* borrow) and writes; `[ARN-1]`'s `alloc*` methods
  take `self` and bump the pointer. Either write them as compiler-known types
  with builtins (as `Array` and `Span` are), or add the `UnsafeCell` `[CELL-9]`
  names. **Decide that once, for both.**
* **`[DSJ-*]`** `assert_disjoint` — deliberately not started. `[DSJ-2]` says
  the proof is "attached to the **returned values**, not to a program point",
  and the type system has no place to carry a fact on a value. Half of it would
  be the API and the runtime comparison with the *fact* unenforced, which is
  the half that matters.
* **`[RNG-4]`** range tracking (D-018). Half of it waits for a generic
  `min`/`max` over a numeric bound, which waits on interface generics.
* **`[TXT-*]`** the string and text model; **`[CTL-3b]`/`[CTL-3c]`** guaranteed
  iteration lowering with the emitted-C assertions.
* **`[DIA-7]`'s shape catalogue with `tests/ui/` snapshots** — a **Phase 2
  exit criterion**. `compiler/ember_diag/src/shapes.rs` holds every shape;
  `tests/ui/` is empty and `[DIA-13]` requires a rendered snapshot per shape
  plus `[PHIL-8a]`'s `.fixed.em` companion.

### 6.3 The error pages, half-built and green

`tools/error_pages.py` exists and works: for each `docs/errors/EXXXX.md` it
runs the ```` ```ember,fails ```` block (must fail with that code) and the
```` ```ember,fixed ```` block (**must compile** — that is `[PHIL-8a]`), and
requires a `## Why` section. Three pages written: **E2210, E2211, E2215**. 59
emitted codes have none and are baselined; the baseline ratchets.

The checker earned its keep immediately: it rejected two of the three pages I
first wrote — one for a trailing `##` doc comment, one for naming `RangeError`
(D-025).

---

## 7. Traps paid for, in this session

* **A trailing `##` is a doc comment**, not a comment: `x = 1  ## note`
  attaches to the next declaration and changes what parses. Use `#`.
  This cost time three separate times.
* **Line endings.** `.gitattributes` pins LF and the Edit tool writes CRLF on
  this host. Normalise before committing; a mixed file makes the *next*
  scripted edit fail silently. `git ls-files --eol | grep "w/crlf\|w/mixed"`.
* **A Python triple-quoted string treats `\<newline>` as a continuation**, so
  a Rust string written that way loses its backslash and keeps the
  indentation. One diagnostic shipped with eighteen spaces in the middle of a
  sentence. Use `concat!`.
* **`option_of` builds `None` first.** Read a variant index from the type
  table, never assume it.
* **The conformance harness requires an `accept_*` case per directory**
  (`[TST-4a]`), and every file must carry `#$ rules:`.
* A module path segment cannot contain `-`, so a conformance test under
  `tests/conformance/MOD-7/` imports its support module relative to the entry
  file (`from support.health import …`), not by a path through the directory.

---

## 8. Where the code is

| Crate | Touched this session |
|---|---|
| `ember_branding` | `STD_PACKAGE`, `std_root()`, `std_path_var()` |
| `ember_lexer` | keyword set 48 → 49 (`yield`), `gen` contextual |
| `ember_ast` | `TypeAlias.range`, `FnDecl.is_gen`, `ExprKind::Yield` |
| `ember_parser` | `range_clause`, `gen fn`, `yield`, `[LEX-6a]`/`E0106`, doc comment above an import, arguments parsed with no block lambda |
| `ember_types` | `TyKind::Range`, `TyKind::Span`, `Bound`, `RangeDef`, `FieldVis`, `StructDef.declaring_module`, `CommonTypes::self_ty`, `substitute_self` |
| `ember_typeck` | range types end to end, spans, `Self`, whole-program interfaces, field visibility, `fn` types, function values, `synth_lambda` |
| `ember_hir` | `EraseRange`, `FnValue`, `CallIndirect`, six new `Builtin`s |
| `ember_mir` | `Const::Fn`, `FuncRef::Indirect`, `lower_range_checked`, `lower_span_get` |
| `ember_analysis` | elision for `SpanFrom`/`SpanGet`/indirect calls |
| `ember_codegen_c` | span and range lowering, function-pointer typedefs |
| `ember_driver` | `std` module resolution, the conformance runner |
| `runtime/ember_rt` | `math.h` (for `[RNG-3a]`'s `fmin`/`fmax`), `ember_span`, `ember_mutspan` |
| `tools` | `rule_index.py` ratchet, `spec_check.py` fourth opt-out reason, `split_spec.py` Part XXIII + the placement table, **`error_pages.py` (new)** |
